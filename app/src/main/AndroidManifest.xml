<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.blazej.languagelearner">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".LearningToPolishActivity"/>
        <activity android:name=".ChooseCategoryActivity" />
        <activity android:name=".LearningToGermanActivity"/>
        <activity android:name=".LearningSummaryActivity"/>
        <activity android:name=".ChoosingToPolishActivity"/>
        <activity android:name=".ChoosingToGermanActivity"/>
        <activity android:name=".ListenActivity"/>
        <activity android:name=".ReviewActivity"/>
    </application>

</manifest>